for 计数器 in range(1000,10001):
    字符 = 计数器
    字符 = str(字符)
    工具人0 = str(0)
    if 字符[1] == 工具人0:
        去0字符 = 字符[0]+字符[2]+字符[3]
        去0字符 = int(去0字符)
        if 计数器/去0字符 == 9:
            print(计数器)